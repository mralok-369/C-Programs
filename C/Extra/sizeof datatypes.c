// size of each data types
#include<stdio.h>
#include<conio.h>
int main()
{
    printf("size of an int = %d bytes",sizeof(int));
    printf("\nsize of a short = %d bytes",sizeof(short));
    printf("\nsize of a long = %d bytes",sizeof(long));
    printf("\nsize of a char = %d bytes",sizeof(char));
    printf("\nsize of a float = %d bytes",sizeof(float));
    printf("\nsize of a double = %d bytes",sizeof(double));

    getch();
    return 0;

}
