#include<stdio.h>
enum week {Sunday, Monday, Tuesday, Wednessday, Thrusday, Friday, Saturday};
int main()
{
    // creating today variable of enum week type
    enum week today;
    taday = Wednessday;
    printf("Day %d",today+1);
    return 0;
}
